{
  "name": "<%= appname %>",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "gulp browser-sync",
    "build": "gulp clean && NODE_ENV=production gulp",
    "lint": "gulp lint"
  }
}
