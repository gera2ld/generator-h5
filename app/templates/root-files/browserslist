# See https://github.com/ai/browserslist#config-file for details

# default list
<%- (target ? utils.commentLines : utils.identity)(`\
last 2 Chrome versions
`) -%>

# compatible with older browsers
<%- (target ? utils.identity : utils.commentLines)(`\
Android 2.3
Android >= 4
Chrome >= 35
Firefox >= 31
Explorer >= 9
iOS >= 7
Opera >= 12
Safari >= 7.1
`) -%>
