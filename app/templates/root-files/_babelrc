{
  "presets": [
    ["env", {
      "modules": false<% if (!target) { %>,
      "targets": {
        "browsers": ["last 2 chrome versions"]
      }<% } %>
    }]
  ],
  "plugins": [
    ["transform-runtime", { "polyfill": false }]
  ]
}
